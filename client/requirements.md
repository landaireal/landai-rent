## Packages
framer-motion | Smooth animations for hero, cards, and page transitions
react-intersection-observer | Scroll-triggered animations for sections

## Notes
Tailwind Config: Add 'Cairo' font for Arabic support and 'Outfit' for English display.
Integration: Images are static imports for logos, but properties use dynamic URLs (placeholders for now).
Bilingual Support: Toggle adds `dir="rtl"` to body/container when Arabic is selected.
